// Configuration for the application
// This can be changed to point to a real backend when available
const config = {
  backendUrl: 'https://api.example.com/familytree',
  // Flag to use mock data instead of real API
  useMockData: true,
};

export default config;